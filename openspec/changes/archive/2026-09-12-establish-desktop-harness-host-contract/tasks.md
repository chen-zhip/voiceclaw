## TDD contract

The public seams below are confirmed: mounted Relay owner HTTP routes, the dedicated `/host/ws` production adapter, the Desktop main-process `DesktopHostRuntime` interface, Desktop settings IPC, Kernel Phase 0 loading, and the provider-neutral Host wire protocol. Tests use loopback HTTP/WSS or in-memory adapters behind those same interfaces; an unmounted class or synthetic profile is not acceptance evidence. Every new RED step must fail for the named absent behavior rather than discovery, syntax, or setup. Existing GREEN evidence is retained only for the two completed foundation modules.

## 1. Host authority and production transport

- [x] 1.1 Public seam: typed durable Host authority records. Extend `relay-server/test/desktop-host/host-enrollment.test.ts` with `preserves typed Host authority records` to assert explicit legacy/enrolled variants and re-registration that preserves unrelated revoked records. RED: run `yarn workspace relay-server vitest run test/desktop-host/host-enrollment.test.ts -t "preserves typed Host authority records"` and confirm the current optional-field/replacement model fails. GREEN: change only the `ControlStateStore` Host union and enrollment upsert. Verify with the same command.
- [x] 1.2 Public seam: mounted owner enrollment HTTP routes. Create `relay-server/test/desktop-host/host-http.test.ts` with `enrolls one remote Host through owner HTTP routes`, using the real Relay HTTP application adapter to assert owner authorization, short expiry, single exchange, and one-time plaintext return. RED: run `yarn workspace relay-server vitest run test/desktop-host/host-http.test.ts -t "enrolls one remote Host through owner HTTP routes"` and confirm the routes are absent. GREEN: mount only enrollment issue/exchange routes over the enrollment module. Verify with the same command.
- [x] 1.3 Public seam: dedicated remote `/host/ws` upgrade. Extend `relay-server/test/desktop-host/host-connection.test.ts` with `admits only a remote Host on the dedicated WSS path`, using a loopback server and real `ws` client to assert subprotocol negotiation, Host-only header authentication after restart, and invalid/expired/revoked/Client/Provider rejection. RED: run `yarn workspace relay-server vitest run test/desktop-host/host-connection.test.ts -t "admits only a remote Host on the dedicated WSS path"` and confirm no Host upgrade path exists. GREEN: implement and mount only the Relay WSS adapter and authenticated Host connection registration. Verify with the same command.
- [x] 1.4 Public seam: bundled-local launch bootstrap. Extend `desktop/src/main/services/relay-server.test.ts` and `host-connection.test.ts` with the shared title `passes one bundled Host bootstrap only through the controlled launch`, asserting Desktop generates one per-startup secret, injects it into Relay and Host runtime environments, the local WSS header authenticates only that stack, and shutdown expires it without persistence or Host Credential issue. RED: run both named test files filtered by that title and confirm production launch wiring is absent. GREEN: implement only bootstrap generation, launch injection, local WSS admission, and expiry. Verify with the same filtered commands.
- [x] 1.5 Public seam: Desktop secure Host Credential and outbound connection. Create `desktop/src/main/desktop-host/host-transport.test.ts` with `stores one Host Credential securely and initiates outbound WSS`, using secure-storage and loopback WSS adapters to assert plaintext is written only to OS secure storage, reconnect uses the Host-only header/subprotocol, and logs/configuration contain no credential. RED: run `yarn workspace voiceclaw-desktop vitest run src/main/desktop-host/host-transport.test.ts -t "stores one Host Credential securely and initiates outbound WSS"` and confirm the Desktop transport is absent. GREEN: implement only the credential adapter and outbound transport inside `DesktopHostRuntime`. Verify with the same command.
- [x] 1.6 Public seam: mounted owner Host management and Desktop projection. Extend `host-http.test.ts` and create `desktop/src/main/desktop-host/host-management-ipc.test.ts` with the shared title `manages a Host without credential disclosure`, asserting owner-only status/last activity/revoke/re-register, durable revocation after restart, immediate work rejection, and secret-free Desktop settings IPC. RED: run both files filtered by that title and confirm mounted management/IPC wiring is absent. GREEN: mount only the Relay management routes and Desktop IPC adapter. Verify with the same commands.

## 2. Assignment, native ownership, and Kernel loading

- [x] 2.1 Public seam: exact Active Host Assignment selection. Extend `relay-server/test/desktop-host/active-assignment.test.ts` with `selects only matching Provider and Workspace readiness`, asserting Relay-only ownership, exact Host/provider/Workspace tuple matching, one selected Host, generation increment, restart-offline state, and no automatic replacement. RED: run `yarn workspace relay-server vitest run test/desktop-host/active-assignment.test.ts -t "selects only matching Provider and Workspace readiness"` and confirm mismatched readiness is currently accepted. GREEN: change only readiness keys and authoritative selection validation. Verify with the same command.
- [x] 2.2 Public seam: assignment/readiness Host control frames. Create `relay-server/test/desktop-host/host-control-frames.test.ts` with `updates assignment state through authenticated Host control frames`, asserting only the authenticated Host may report readiness and Relay alone emits authoritative assignment/generation frames over `/host/ws`. RED: run the file and confirm control-frame routing is absent. GREEN: add only assignment/readiness frame handling to `DesktopHostGateway`. Verify with the same command.
- [x] 2.3 Existing GREEN seam: `NativeProviderConfigurationService` validates and persists Workspace paths, executable settings, preferences, and secret references behind its storage adapter while projecting only schema-approved non-secret readiness. Evidence: `yarn workspace voiceclaw-desktop vitest run src/main/desktop-host/native-provider-configuration.test.ts`.
- [x] 2.4 Public seam: production Desktop settings persistence and IPC. Extend `native-provider-configuration.test.ts` with `uses Desktop settings storage behind secret-free IPC`, asserting the production settings adapter survives runtime reconstruction and IPC never returns paths, arguments, preferences not approved by schema, or secret material. RED: run the file filtered by that title and confirm production storage/IPC assembly is absent. GREEN: add only the settings adapter and IPC methods behind `DesktopHostRuntime`. Verify with the same command.
- [x] 2.5 Existing GREEN seam: `ProviderProcessSupervisor` distinguishes executable/process/transport/Session readiness and terminates only started or explicitly adopted processes. Evidence: `yarn workspace voiceclaw-desktop vitest run src/main/desktop-host/provider-process.test.ts -t "terminates only an owned process"`.
- [x] 2.6 Public seam: Desktop main-process lifecycle. Create `desktop/src/main/desktop-host/desktop-host-runtime.test.ts` with `starts and stops only Desktop-owned Host resources`, asserting main-process startup connects after configuration and Contribution loading, while shutdown closes WSS, expires local bootstrap, and stops only owned/adopted Provider processes. RED: run the file and confirm runtime assembly is absent. GREEN: implement only `DesktopHostRuntime.start/stop/status` and wire it into Desktop main startup/shutdown. Verify with the same command.
- [x] 2.7 Public seam: real Kernel `provider-integration` loading and readiness. Extend `relay-server/test/desktop-host/provider-registration.test.ts` with `loads Host Contributions through Kernel Phase Zero`, using an allowlisted package fixture to assert recognized Harness/Profile versions, contracts, grants, independent Contribution states, and provider readiness arrive through authenticated Host registration; settings stays `ACTIVE` while provider becomes `DEGRADED`. RED: run the file filtered by that title and confirm the current in-memory registry is not Kernel-loaded. GREEN: integrate registration with `PhaseZeroKernel` and its effective graph only. Verify with the same command.

## 3. Authorized incremental invocation lifecycle

- [x] 3.1 Public seam: authorized operations over `/host/ws`. Extend `relay-server/test/desktop-host/invocation-stream.test.ts` with `authorizes every Host operation before writing a frame`, asserting all four `harness.execution@1` operations require an authenticated, unrevoked Host, current assignment, and Kernel grant for operation and Scope. RED: run the file filtered by that title and confirm non-cancel operations default open. GREEN: require the Kernel authorizer and current Host/assignment lookup in `DesktopHostGateway`; remove permissive defaults. Verify with the same command.
- [x] 3.2 Public seam: incremental provider-neutral stream frames. Extend `invocation-stream.test.ts` with `forwards validated public events incrementally`, using loopback WSS to assert layered identities, increasing sequence, immediate Semantic Output visibility before terminal, exactly one terminal, bounded provider-neutral frames, and no Provider-native/private fields. RED: run the file filtered by that title and confirm `turn.start` currently buffers a completed array. GREEN: implement only incremental request/event/terminal frame handling. Verify with the same command.
- [x] 3.3 Public seam: live cancellation routing. Extend `invocation-stream.test.ts` with `routes cancellation only to the live owning Attempt`, asserting authorized matching cancellation reaches the selected Contribution while unauthorized/mismatched/post-terminal cancellation and Semantic Output are rejected. RED: run the file filtered by that title and confirm cancellation is not integrated with the WSS Attempt registry. GREEN: add only cancellation lookup and routing. Verify with the same command.
- [x] 3.4 Public seam: Relay-authoritative assignment and stream fence. Extend `invocation-stream.test.ts` with `fences every frame against Relay Control State`, mutating the authoritative assignment between frames to assert stale envelopes/events/terminals, duplicate or out-of-order sequence, invocation/Attempt mismatch, terminal-late event, and second terminal are rejected. RED: run the file filtered by that title and confirm a self-consistent stale envelope currently passes. GREEN: read the current assignment generation at dispatch and every receive boundary and integrate the shared fence. Verify with the same command.
- [x] 3.5 Public seam: transport disconnect outcome. Extend `relay-server/test/desktop-host/disconnect-outcome.test.ts` with `records disconnect outcome from the live transport`, closing loopback WSS before and after dispatch to assert `failed` versus `unknown`, preserved input and accepted public prefix, one terminal, and no replay or Host substitution. RED: run the file filtered by that title and confirm disconnect is only an isolated in-memory method. GREEN: connect transport close to the live Attempt registry and Routing observer only. Verify with the same command.

## 4. Production-profile acceptance and regression

- [x] 4.1 Public seam: remote production Host Profile. Rewrite `relay-server/test/integration/desktop-host-minimal-profile.test.ts` around the production Relay application and Desktop `DesktopHostRuntime` loopback adapters, with `starts the remote minimal Host Profile`, asserting HTTP enrollment, secure credential use, outbound `/host/ws`, Kernel-loaded granted `harness.execution@1`, readiness/assignment, and one streamed Turn without Archive/Memory or content in `ControlStateStore`. RED: run the file filtered by that title and confirm the current synthetic profile cannot satisfy it. GREEN: assemble only the real remote provider-neutral Host path. Verify with the same command.
- [x] 4.2 Public seam: bundled-local production Host Profile. Extend the same integration file with `starts the bundled-local minimal Host Profile`, asserting controlled bootstrap launch, outbound `/host/ws`, readiness/assignment, and one streamed Turn without Host Credential issuance, Archive/Memory, or content persistence. RED: run the file filtered by that title and confirm production local assembly is absent. GREEN: assemble only the bundled-local path. Verify with the same command.
- [x] 4.3 Non-TDD regression and review gate: run `yarn workspace relay-server vitest run test/desktop-host test/integration/desktop-host-minimal-profile.test.ts`, `yarn workspace voiceclaw-desktop vitest run src/main/desktop-host src/main/services/relay-server.test.ts`, `yarn workspace relay-server vitest run test/plugin-kernel test/integration/plugin-kernel-minimal-profile.test.ts test/integration/plugin-kernel-phase-zero.test.ts`, `yarn workspace @voiceclaw/contracts vitest run`, and `yarn typecheck:all`; inspect the effective graph, mounted route table, control-state document, Desktop startup/shutdown, and source diff; verify Archive, Memory, multi-Host failover, Native TUI Handoff, automatic credential rotation/recovery, third-party isolation, and general plugin installation remain absent; then run `$code-review` from `1f855d75fd58efc9a05cab12014473f1969d5b87`, resolve all findings, and rerun `openspec validate establish-desktop-harness-host-contract --strict`.
